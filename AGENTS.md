# 維護指南

本文件供 AI 與維護者修改 codex-switch 時使用。保持精簡，不複製使用手冊
或逐次工作紀錄；不依賴原作者主機上的私人文件。

## 文件分工與閱讀順序

1. 本文件：修改範圍、安全規則、不可任意改變的行為與驗證流程。
2. [README.md](README.md)：精簡的使用者入口，只放現行功能、安裝、核心工作流
   與必要限制；不放輸出逐行教學、內部實作細節或逐次發布紀錄。
3. [VERIFICATION.md](VERIFICATION.md)：按時間／版本保存的驗證證據與未驗證項目。
   舊版本紀錄不是目前操作規格；歷史測試通過不代表本次變更已驗證。

依任務讀相關章節和實作。發現文件與程式不一致時先指出差異，再依使用者
要求修正；不要根據歷史紀錄恢復已被取代的行為。

## 程式地圖

| 路徑 | 責任 |
| --- | --- |
| `bin/codex-switch.mjs` | CLI 入口 |
| `src/main.mjs` | 參數解析、指令流程、文字／JSON 輸出 |
| `src/display.mjs` | 本地時間格式、終端色彩與健康標記（不改資料） |
| `src/core.mjs` | 帳號池、身分比對、權限／鎖、Codex RPC 與額度判斷 |
| `src/auto.mjs` | 原生登入手動切換與獨立監控，共用憑證替換流程 |
| `src/live.mjs` | 實驗性 `run --auto` 會話與即時切換 |
| `test/` | 暫存帳號池、合成憑證與假 Codex 的回歸測試 |
| `scripts/verify-live-protocol.mjs` | 真實 Codex CLI 搭配本機模擬服務的協定驗證 |

這是獨立的 Node.js ES module repository；不需要 Python 或其他專案環境。
執行指令前先確認目前目錄、`git status` 和 `git remote -v`，保留無關修改。

## 必須保留的行為

- `use` 寫入原生 home 的登入檔，也更新工具預設選擇；不是只改池內標記。
  原生 home 依 `--codex-home`／`CODEX_HOME`／`~/.codex` 選定。
- `auto` 查原生登入身分與額度，沿用手動切換流程；不啟動、接管或確認既有
  session，不重送工作、不印通知。`run --auto` 是另一個實驗性模式。
- `list`／`usage` 的 `*` 來自實際原生登入檔，不等於工具的預設選擇。
  JSON 的 `active` 與 `selected` 不可混用；未知身分不可沿用舊標記。
- 身分比對依登入檔的 `sub` 與 `account_id`，不是 email 或 plan。
  `login` 同名同身分可更新授權；不同身分不能覆蓋。`import` 是獨立快照，
  拒絕既有名稱／重複身分，不是永久授權，也不是同名憑證更新指令。
- `rename` 不搬動 home；`remove` 只移出帳號池，保留可復原資料，不登出、
  撤銷或抹除憑證。不得因目錄名稱看似過期就刪除仍被引用的 home。
- 文字額度顯示剩餘 `% left`；JSON 保留原始額度欄位與既有結構。
  額度未知、過期、帳號忙碌或無可用替代者時，不推測可用性強制切換。
- `usage --all` 固定最多同時查兩個獨立帳號，仍逐一取得最新資料；不提供
  使用者調整並行數的參數，也不因平行化而改用額度快取或改變輸出順序。
- 除非明確要求，保持指令、退出碼與 JSON 相容。顯示設計不可改動業務邏輯。

## 安全與權限

- 程式與帳號資料分開。預設帳號池為 `~/.codex/account-pool/`，可由
  `CODEX_SWITCH_HOME` 覆寫；不要把真實帳號池放進 repository。
- 不讀出／印出／提交 token、完整 auth.json、私人對話或憑證備份。
  診斷資訊須遮蔽機密；合成測試資料不可使用真實憑證。
- 自動測試只用明確隔離的暫存目錄與合成帳號。未經使用者授權，不操作真實
  login/logout/use/remove，也不為驗證而啟動真實帳號的自動監控。
- 保留目錄擁有者、檔案模式、符號連結防護、鎖、備份與原子替換檢查。
  取得鎖後重新驗證註冊身分；不要自動搶鎖或盲目清除殘留鎖。
- 遇到權限／認證問題，停止受阻操作並回報，等待使用者處理；不要自行 sudo、
  改權限、切換身分、改快取／認證路徑或改用其他傳輸方式繞過。
  此規則針對維護操作，不撤銷 `use`／`auto` 已明確記載的目錄權限收緊行為。

## 修改、驗證與交付

- 動手前寫明本次驗收條件；保持修改集中，不順便重構或新增依賴。
- 首次安裝／依賴變動：`npm ci --ignore-scripts`。程式修改後執行 `npm test`；
  修 bug 時補能捕捉該問題的測試。額外驗證按風險選擇，不重複無關測試。
- 涉及即時協定時，依 README 執行本機協定驗證；正式服務的串流、刷新與
  無縫切換仍需另行授權及實測，不能由 mock 測試推論成功。
- 純文件修改：檢查差異、相對連結、路徑與指令是否符合實作，執行
  `git diff --check`；不為此操作真實帳號或重跑完整程式測試。
- 使用者可見行為變更更新 README；有新的測試證據或限制才新增驗證紀錄，
  寫清命令、結果與未驗證範圍。不要改寫舊紀錄讓它看似新版結果。
- 提交前檢查 staged diff，只納入本次檔案。commit／push 遵循當次使用者
  授權；發布前確認 remote 與待推提交，不 force-push、不自行改 LICENSE。
- 交付說明修改內容、驗證結果、未驗證事項與 commit／push 狀態。
