{
  "name": "codex-switch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": { "test": "node --test" },
  "bin": { "codex-switch": "bin/codex-switch.mjs" }
}
